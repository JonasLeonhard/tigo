package components

templ Button(itype string) {
	<button
		type={ itype }
		class="w-full text-white border border-orange-900 bg-orange-600 hover:bg-orange-700 focus:ring-4 focus:outline-none focus:ring-orange-300 font-medium text-sm px-5 py-2.5 text-center"
	>
		{ children... }
	</button>
}
